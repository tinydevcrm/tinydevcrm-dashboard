// Base dashboard layout (sidebar, navbar, empty content, etc.)

import React from 'react';

import styles from '_DashboardBase.module.scss';

const DashboardBase = () => (
    <h1>hi</h1>
);

export default DashboardBase;
